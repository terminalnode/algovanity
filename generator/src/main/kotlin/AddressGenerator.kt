package algo.terminal.algovanity.generator

import algo.terminal.algovanity.utils.createLogger

class AddressGenerator {
	val logger = createLogger()

	init {
		TODO("Create coroutine-based algo address generator")
	}
}
